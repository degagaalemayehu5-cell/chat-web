{
  "name": "codesistency",
  "version": "1.0.0",
  "type": "module",
  "main": "backend/src/server.js",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "build": "npm install && npm install --prefix frontend && cd frontend && npm run build",
    "start": "node backend/src/server.js"
  }
}
